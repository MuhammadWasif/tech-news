type Query {
  message: String!
  user(username: String!): User
}

type Mutation {
  createUser(username: String, email: String, password: String): User!
}

type User {
  id: ID!
  username: String!
  email: String!
  createdAt: String!
}
